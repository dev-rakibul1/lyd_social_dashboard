import ResetPasswordCom from "@/components/login/resetNewPassword/ResetPasswordCom";

const ResetNewPassword = () => {
  return (
    <div>
      <ResetPasswordCom />
    </div>
  );
};

export default ResetNewPassword;
